version: '3.8'

services:
  api:
    build: .
    container_name: urbano_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app  
    environment:
      - DATABASE_URL=sqlite:///./logitrack.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
